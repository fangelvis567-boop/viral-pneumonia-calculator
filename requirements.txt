# 病毒性肺炎重症风险计算器 - Web 应用依赖清单
# 适配 Python 3.11 (runtime.txt 锁定)
# 严格锁版本 — 避免 sklearn 升级导致 pkl 反序列化字段不兼容 (例如 _fill_dtype 缺失)

streamlit>=1.30.0          # Web UI 框架（向后兼容良好，可不锁）
scikit-learn==1.7.2        # ⚠️ 必须锁定 — 与训练 pkl 版本一致（IterativeImputer 内部字段会随版本变化）
xgboost==3.2.0             # ⚠️ 必须锁定 — Booster 序列化字段随版本变化
shap==0.51.0               # SHAP 单例解释（与 xgboost 3.2 配套）
joblib>=1.3.0              # 模型反序列化
pandas>=2.0.0              # 数据处理
numpy>=1.24.0,<2.0.0       # 数值计算（限制 <2.0 避免与 sklearn 1.7.2 编译产物的二进制兼容问题）
matplotlib>=3.7.0          # 自绘瀑布图、贡献条形图
