# 病毒性肺炎重症风险计算器 - Web 应用依赖清单
# 适配 Python 3.11 (runtime.txt 锁定)
# 用约束区间替代严格锁版本，便于 pip 找到可用 wheel；同时把 sklearn 限定在 1.7 minor 范围内
# 以匹配训练 pkl 的 IterativeImputer 字段（避免 _fill_dtype 字段不兼容）

streamlit>=1.30.0          # Web UI 框架
scikit-learn>=1.7.0,<1.8   # ⚠️ 限制 1.7.x 以匹配训练 pkl 的 IterativeImputer 字段集合
xgboost>=2.1,<4.0          # XGBoost Booster 在 2.x → 3.x 内部反序列化兼容性较好
shap>=0.44.0               # SHAP 单例解释
joblib>=1.3.0              # 模型反序列化
pandas>=2.0.0              # 数据处理
numpy>=1.26.0              # 数值计算（不限制上限，让 pip 自动适配 sklearn/xgboost wheel）
matplotlib>=3.7.0          # 自绘瀑布图、贡献条形图
